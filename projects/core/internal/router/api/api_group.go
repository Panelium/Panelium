package api

import "github.com/gin-gonic/gin"

func AttachAPIRouterGroup(r *gin.Engine) {
	//moduleGroup := r.Group("/api")
	//pingServiceRoute, pingServiceHandler := pingv1connect.NewPingServiceHandler()
	//moduleGroup.POST(pingServiceRoute, gin.WrapH(pingServiceHandler))
}
